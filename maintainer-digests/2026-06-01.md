# Weekly Maintainer Digest — 2026-06-01

> Automated maintenance pass. Nothing here is merged, released, or posted to public issues.
> Speculative work lives on `claude/` branches only.

## Executive Summary

- **Built** this week: `--format md/markdown` human-readable output for `council run` (Final 9.0) — because it cleared the value gate as a clean, self-contained, fully-tested win.
- **Planned**: nothing deferred — the top candidate qualified and was built.
- **Nothing else** shipped — no merge, release, tag, or public PR; feature lives on a `claude/` branch awaiting human review.

---

## Step 1 — Repo Health

- **Build/install**: `pyproject.toml` well-formed (hatchling, `requires-python>=3.10`; deps: pydantic, httpx, typer, rich, pyyaml, jsonschema, aiosqlite). `pip install -e ".[dev]"` succeeds. Current version **0.7.16** (matches latest release/CHANGELOG; `__version__` resolved from package metadata).
- **Tests**: **371 passed, 0 failed/errored** in ~9.6s (16 files, 372 test fns) on a clean checkout.
- **Lint/type**: `ruff check src/` — **0 violations**. `mypy src/llm_council` — **78 errors / 14 files**, all long-standing/structural (37 `misc`, 21 missing-stub/import, 11 typer `untyped-decorator`, 6 `no-any-return`). Not regressions, but strict-mypy is *not* actually clean.
- **Open issues**: **0 open** on `sherifkozman/the-llm-council`. CLAUDE.md still references issues #26–#35, none of which are live — stale.
- **Stale `claude/` branches**: none to clean (only the active working branch existed remotely).
- **Doc consistency**: **Drift** — CLAUDE.md header declares **v0.6.0** while code/releases are at **v0.7.16** (10+ releases ahead). CLAUDE.md should be refreshed.

**HEALTH: YELLOW** — Build installs, all tests pass, ruff clean; but 78 structural mypy errors (contradicting the "mypy strict" claim) and CLAUDE.md doc drift (v0.6.0 vs actual v0.7.16) are minor, non-blocking issues.

---

## Step 2 — Feature Candidates (ranked)

| Rank | Feature | Source | R | I | C | E | RICE | Final | Self-contained? | Notes |
|------|---------|--------|---|---|---|---|------|-------|-----------------|-------|
| 1 | `--format md/markdown` human-readable output for `council run` | Codebase gap (main.py:596-621) | 7 | 2 | 90% | 0.4 | 31.5 | **9.0** | Yes | Non-JSON output only ever produced raw JSON / a Rich panel; `--output` always wrote raw JSON. A `--format` flag + Markdown formatter (config `output_format` hook half-wired at main.py:489). Pure CLI+formatter, unit-testable. |
| 2 | Shell completion install command (`council completion install/show`) | Missing capability (typer supports it) | 6 | 1 | 90% | 0.4 | 13.5 | 7.0 | Yes | Expose typer/click-generated completions + docs. Low risk, no provider deps. |
| 3 | Default model pricing table → non-zero `cost_estimate` | Codebase gap (orchestrator.py:2457, rates default 0.0) | 6 | 2 | 60% | 0.8 | 9.0 | 6.0 | Mostly | `--verbose` cost is effectively always $0. Add a built-in pricing map + config override. Prices drift (lower confidence). |
| 4 | `doctor --json` exit-code / machine-summary hardening | Codebase polish (doctor cmd) | 5 | 1 | 75% | 0.5 | 7.5 | 5.5 | Yes | Aggregate `all_healthy` + version field for CI gating. Modest impact. |
| 5 | `config --unset KEY` + `--list-keys` | Codebase polish (config cmd ~1546) | 4 | 0.5 | 85% | 0.4 | 4.3 | 4.0 | Yes | Small ergonomic gap (only get/set/edit exist). Low impact. |
| 6 | Adaptive cost/quality routing | ROADMAP (High) | 8 | 3 | 40% | 4.0 | 2.4 | 3.0 | No | High value but large/cross-cutting; needs design + eval. Not a one-run job. |

**TOP:** `--format md/markdown` output | Final **9.0** | Clean self-contained win, buildable end-to-end with unit tests in one run; no provider/network deps.

---

## Step 3 — Value Gate

Threshold = **8** (documented default; no override substituted). Top candidate evaluated:

- Final **9.0** ≥ 8 ✅
- Not flagged (no multi-run flag history) ✅
- Self-contained, completable cleanly in one run ✅

**Gate: PASSED → proceed to build.**

---

## Step 4 — What Was Built

**Branch:** `claude/feature-markdown-output-2026-06-01` (pushed to origin; **no PR opened**).

- `src/llm_council/cli/main.py` — `_render_result_markdown(...)` renders a `CouncilResult` to clean Markdown (status heading, Output/Errors, Critique, Metrics, Routing). New `--format` option (`json` | `markdown`/`md`) with precedence `--format > --json > config output_format`, wired into both stdout and `--output` file writes. Default (no flag) still produces the rich panel; `--json` unchanged (now maps to `--format json`).
- `tests/test_cli.py` — `TestMarkdownOutputFormat` (8 tests): formatter success/failure, `--format markdown`/`md` stdout, `--format json` parity, invalid-format exit, `--output` write, config-driven `output_format: markdown`.

**Verification:** `379 passed` (full suite); ruff clean; mypy unchanged (78 pre-existing, 0 new). Self-contained as vetted — no orchestrator/protocol changes.

---

## Stale `claude/` Branch Cleanup List

**None.** Only the active working branch existed remotely; no abandoned/superseded `claude/` branches to delete. (The two new branches created this pass — `claude/feature-markdown-output-2026-06-01` and `claude/weekly-maintainer-2026-06-01` — are current.)

---

## Notes / Follow-ups (not actioned this pass)

- **Slack post skipped**: the target channel was an unsubstituted placeholder (`#<channel>`), so the 3-line summary was not posted anywhere — posting to a guessed channel is an outward-facing action and was deliberately avoided.
- **Doc hygiene (next pass candidate)**: refresh CLAUDE.md to v0.7.16 and drop the stale #26–#35 issue references.
- **mypy debt**: 78 structural errors persist; worth a dedicated cleanup pass (typer decorator typing + `types-PyYAML`) if "strict mypy" is a real goal.
