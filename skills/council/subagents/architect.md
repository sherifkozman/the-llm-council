# Architect (Legacy Alias)

`architect` is a backwards-compatible alias.

Prefer:

```bash
council run drafter --mode arch "<task>"
```

Use it for:

- system design
- API and data model design
- component boundaries and tradeoffs

Example:

```bash
council run drafter --mode arch "Design a multi-tenant SaaS API with row-level security" --json
```
